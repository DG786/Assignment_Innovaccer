CREATE DATABASE IF NOT EXISTS hackercamp;
USE hackercamp;
create table fnrtend(id varchar(100) NOT NULL,category varchar(100), amount varchar(100) NOT NULL, expense_day varchar(100) NOT NULL, expense_time varchar(100) NOT NULL , createdAt varchar(100) NOT NULL, reason varchar(100) NOT NULL , PRIMARY KEY(id));